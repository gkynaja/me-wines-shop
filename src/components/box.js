import styled from 'styled-components'
import {
  margin,
  padding,
  display,
  height,
  maxHeight,
  maxWidth,
  minHeight,
  minWidth,
  overflow,
  size,
  verticalAlign,
  width,
} from '../styles/styled-system'

const Box = styled.div`
  ${margin}
  ${padding}
  ${display}
  ${height}
  ${maxHeight}
  ${maxWidth}
  ${minHeight}
  ${minWidth}
  ${overflow}
  ${size}
  ${verticalAlign}
  ${width}
`

export default Box
